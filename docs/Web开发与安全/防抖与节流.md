---
title: 防抖与节流
description: 防抖和节流是JavaScript中处理高频事件（如用户交互、滚动、调整大小）的常用优化技巧。
sidebar_position: 2
tags: [Web]
date: 2025-11-28
---

# 防抖与节流
防抖和节流是JavaScript中处理高频事件（如用户交互、滚动、调整大小）的常用优化技巧，用于避免函数过度执行导致性能问题。当函数绑定一些持续触发的事件如：`resize`、`scroll`、`mousemove` ，键盘输入，多次快速`click`等等，如果每次触发都要执行一次函数，会带来性能下降，资源请求太频繁等问题。

## 防抖Debounce
### 概念
将高频事件合并为一次执行，等待事件停止触发一段时间（延迟时间）后才执行函数。适合“最后一次”才处理的场景。

防抖的原理是在事件被触发后，在指定的时间内如果事件没有再次被触发，则执行一次函数；如果在这段时间内事件再次被触发，则重新计时。简单来说，就是将多次触发的事件合并为一次执行，常用于需要等待用户操作完全停止后再进行处理的场景，如输入框搜索、窗口大小调整等。

### 代码
```js
function debounce(func,delay){
   return function(args){
      clearTimeout(func.id) //清除定时器  如果注释的话还是那么多频次，只是每次推迟了
      // 函数也是对象  id挂在func上，func是闭包中的自由变量  
      func.id = setTimeout(function(){
          func(args)
      },delay)
   }
}
```
我们定义一个实现防抖的函数，接受两个参数，一个为我们执行的事件函数，另一个参数就是我们定义的延迟的时间，这里利用了闭包这个思想。

`clearTimeout(func.id)`这行代码的就是清除之前的定时器，要实现防抖，就是需要确保在一段连续触发的相同事件中取最后一次且经过delay时间后才触发。

`func.id = setTimeout`这就是设置个定时器，经过delay的时候才会触发这个函数。 这样就实现了我们想要的防抖功能，因为每次事件触发都会重置定时器，而 func 只会在定时器最终未被清除的情况下执行。

## 节流Throttle
### 概念
限制函数执行频率，在指定时间间隔内只执行一次，适合“均匀间隔”执行的场景。

节流的原理是在规定的时间间隔内，无论事件触发了多少次，都只执行一次函数。也就是说，它会确保函数以一定的频率执行，而不是无限制地响应事件触发。节流常用于需要频繁但有规律地执行某些操作的场景，如滚动监听、鼠标移动等。

### 代码
```js
const throttle = (func,delay) =>{
    let last,deferTimer //闭包中的自由变量，表示上一次执行的时间，以及定时器
    return (args)=>{
       // 干掉触发
       let now = +new Date();
       if(last && now <last+delay){
          clearTimeout(deferTimer)
          deferTimer = setTimeout(()=>{
             last = now
             func(args)
          },delay)
       }else{
          //需要让执行过段时间再来到此处
          last = now  // 第一次时间
          func(args)  // 先执行一次
       }
    }
}
```
`if(last && now <last+delay)`判断当前时间与上一次执行的时间和延迟的时间大小，如果小于则清除掉事件的定时器，设置一个定时器确保最后一次的事件能够被执行。否则的话就允许执行。这个逻辑还是比较简单的。

## 总结
简单来说，防抖就是回城，节流就是放技能。

### 防抖 vs 节流 —— 游戏类比版

| 概念   | 游戏比喻          | 触发频繁时怎么处理                     | 什么时候执行             | 核心思想               |
|--------|-------------------|----------------------------------------|--------------------------|------------------------|
| **防抖** | 回城              | 每次触发都**取消上一次**，重新计时     | **等用户彻底停下来**     | “等你消停了再说”       |
| **节流** | 放技能（CD）      | **不管你怎么按**，固定间隔执行一次     | **每隔一段时间执行一次** | “有冷却时间”           |

## 参考
- [**防抖节流，必须必拿下！**](https://juejin.cn/post/7386969940941586466?searchId=202604141041057981497F67E28760DBC0)
- [**前端性能优化利器——防抖与节流**](https://juejin.cn/post/7397278180656611369?searchId=202604141041057981497F67E28760DBC0)
- [**前端性能优化篇： 防抖和节流**](https://juejin.cn/post/6958704784626941982?searchId=202604141041057981497F67E28760DBC0)