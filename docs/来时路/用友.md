---
title: 用友
description: 用友面经。
sidebar_position: 103
draft: true
date: 2026-09-03
---

# 用友
- 岗位：web全栈开发工程师（高潜）
- base：北京

## 一面
- 日期：9.3
- 时间：47分钟

### 八股
- 浏览器url加载过程：
    - 请看[在浏览器中输入URL按下回车后发生了什么](./计算机网络八股#在浏览器中输入URL按下回车后发生了什么)
- 浏览器缓存策略：
    - 请看[与缓存相关的HTTP请求头](./计算机网络八股#与缓存相关的HTTP请求头)
- 回流和重绘，如何减少？
    - 请看[回流与重绘](./八股再整理#回流与重绘)
- 跨域，如何解决？
    - 请看[跨域](/docs/计算机网络/跨域)
- hash router与history router的区别和使用场景：
    - 请看[对React Router的理解](./八股再整理#对react-router的理解)
- 目前业界哪种路由方式用的更多？
```txt
现实中还是history router用的更多，hash router主要用在部署受限的项目和一些后台管理系统中。

history router常用于：
- 正式对外的网站
- 电商、内容平台、SaaS 产品
- 需要 SEO 的页面
- React/Vue 单页应用
- 有能力配置 Nginx、CDN 或服务器的平台
- Next.js、Remix 等支持服务端渲染的框架

history router的url更自然，更适用于：
- SEO
- SSR
- 服务端路由
- 日志分析
- 分享链接
- 与后端路由规则结合
不过history router需要配置SPA fallback，防止在找不到文件时返回404

hash router常见场景：
- 无法修改服务器配置
- 直接放在 CDN 或对象存储上的静态页面
- 内部后台管理系统
- Electron 等桌面应用
- 嵌入第三方系统的页面
- 老项目
- 临时演示或原型
- 通过本地文件打开的应用

hash router的缺点是：
- URL 中包含 #
- SEO 支持较差
- 服务端无法直接获取 # 后面的路径
- 不适合 SSR
- 与服务端路由结合不自然
```
- 介绍下cookie、LocalStorage、SessionStorage的区别：
    - 请看[前端本地数据缓存有哪些方式](./八股再整理#前端本地数据缓存有哪些方式)
- vite与webpack的区别：

| 区别维度                 | Vite                                     | Webpack                                                 |
| -------------------- | ---------------------------------------- | ------------------------------------------------------- |
| **开发启动速度**           | **快**，基于原生 ES 模块动态导入，无需全量构建，首次启动和热更新速度极快 | **慢**，需要全量构建项目，首次启动和热更新速度相对较慢                           |
| **热更新速度**            | **快**，几乎即时更新，用户体验好                       | **慢**，尤其是在大型项目中，更新速度可能受限                                |
| **打包速度**             | **快**，生产环境使用 **Rollup**，适合大型项目           | **慢**，但可通过配置优化（如缓存、并行构建）提升速度                            |
| **构建原理**             | 开发阶段基于原生 ES 模块动态导入，生产阶段使用 **Rollup** 打包  | 基于 **Node.js**，通过 **Loader** 和 **Plugin** 对资源进行解析、转换和打包 |
| **配置复杂度**            | **低**，默认配置满足大多数需求，无需手动配置                 | **高**，需要手动配置 **Loader** 和 **Plugin**，但自定义能力强            |
| **对 TypeScript 的支持** | **内置支持**，无需额外配置                          | 需配置 **ts-loader** 或 **babel-loader** 等，配置稍复杂            |
| **对 CSS 的支持**        | **内置支持**，包括 CSS 预处理器                     | 需配置 **css-loader**、**sass-loader** 等，配置灵活但复杂            |
| **插件生态系统**           | **较新但发展迅速**，插件与 **Rollup** 插件生态相似        | **成熟且庞大**，有大量的插件可供选择                                    |
| **兼容性**              | **现代浏览器** 支持好，但老旧浏览器可能需要额外配置（如 Babel）    | **兼容性极强**，通过配置可支持几乎所有浏览器                                |
| **静态资源处理**           | **内置支持**，通过 `import` 导入图片等资源             | 需配置 **file-loader**、**url-loader** 等，配置灵活但复杂            |

### 项目
- 数据资源树是什么
```txt
ResourceTree是前端对后端资源目录的内存镜像，ResourceNode 是其中的单个节点。
就目前代码的数据结构而言，资源树真正是一棵有根树，不是图；只是额外维护了一张Map（Scene）作为节点索引。
它是一棵懒加载树，只有在展开节点时才调用后端接口获取直接子节点，不展开的分支不会立即加载完整后代。

ResourceNode同时承载了三类信息：
- 树结构
- 资源身份和类型
- 运行时编辑会话

这么看来ResourceNode不是单纯的树节点，更像是：
资源目录节点 + 资源类型 + 编辑会话容器

ResourceTree管理整棵资源树以及UI状态，树决定了节点间的层级关系，
而scene这个map则实现从O(n)到O(1)查找优化，可以通过节点的key快速找到对应节点。
```
- 节点懒加载是什么？
```txt
节点懒加载是指前端在初始化时，只请求加载根节点及其直接信息，不一次性全量请求整棵树，只有用户展开目录时才请求该节点的子节点。

每个节点有一个aligned属性，初始为false，当请求过一次后会将其设置为true，并将请求的子节点加入到其children属性中，后续再进行折叠展开，不会重复请求后端，而是直接使用内存中该节点的children。scene这个Map索引结构也只存加载出的节点。

为什么采用懒加载：
- 首屏只请求少量节点，让资源树初始化更快
- 避免巨量资源节点渲染
- 减少前端内存占用
- 用户不访问的目录不占用网络请求
- 每个目录可以单独刷新和同步：当在目录下做节点增删改时，只对该路径与后端做数据对齐，而不用对整棵树全量更新。

代价是：每次展开目录需要做短暂等待
```
- 发布订阅设计模式在其中怎么使用的？
```txt
本质上ResourceTree和ResourceNode都只是普通可变class对象new出的实例，他们无法驱动React重新渲染。
这里通过发布订阅机制，实现了一个轻量级的“状态变化通知器”。

ResourceTree通过一个Set来订阅更新回调，用Set的好处在于：
- 支持多个组件同时订阅；
- 相同回调不会重复注册；
- 删除订阅的成本较低。

在对应的左侧栏资源树组件中，用useReducer通过结构赋值，定义了一个强制刷新的dispatch reducer即triggerRepaint，用触发便state自增的方式来手动驱动一次React渲染。
```
- 发布订阅模式的底层机制
- 节点如何响应订阅trigger

### AI使用范式
- 目前是怎么使用AI来辅助工作的
- 对于代码标准化如何约束AI
- 举例说明AI输出的代码有什么缺陷
- 如何应对这些AI输出的缺陷

### 手撕
力扣1556.千位分隔数


## 二面
- 日期：9.9
- 时间：65分钟

### 个人情况
- 2-3分钟自我介绍
- 前后端主要技术栈
- JS与TS实际使用时间
- 前端三件套具体是什么？常用哪些前端框架？

### AI使用情况
- 现在 AI Coding 已经很成熟，尤其擅长生成前端代码。你怎么看、怎么评价 AI Coding？
- 什么是 Spec Coding？
- 17:14 不谈工具流程本身：结合前端开发，怎样写出高质量 Spec，才能让 AI Coding 更高效、少出错、符合技术栈与交互目标？
   - 给出一个具体前端需求的 Spec 示例
   - 怎样约束角色、页面结构、状态管理、交互、校验、验收标准？
   - 你在实际项目中有哪些具体心得，而不只是通用方法论？

### 八股场景拷打
- 了解 Java 吗？除 TypeScript 外还了解哪些语言？
- JavaScript / TypeScript 从源码到执行，大致经历哪些过程？解释器 / 编译器如何工作？
- 常见的 JavaScript / TypeScript 转译、构建工具有哪些？
- this 是如何工作的？有哪些绑定方式？
- 箭头函数中的 this 能重新绑定吗？为什么？
- 解释原型链。
    - `__proto__`与 `prototype` 的区别是什么？
    - 哪些对象拥有 prototype 属性？
- package.json 中的 exports 有什么作用？
    - import / export 在模块组织、打包和解析中如何工作？
- 读过第三方库源码吗？关注过它们的工程结构吗？
- 做全栈 / 前端开发真正的兴趣点在哪里？
- 你平时如何深入研究一项技术，而不只停留在官方文档或表层 API？
    - 如何把文档里的内容快速用于真实项目？
    - 会不会读源码、查深度资料？
- 你更关注React Hook 怎么用，还是 Hook 的运行原理？
- 自定义 Hook 是什么、解决什么问题？
    - 是否只能包装原生 Hook？
    - 不使用原生 Hook 时，写出的函数与自定义 Hook 有什么区别？
- 常用哪些项目脚手架？自己封装过脚手架吗？
- 前后端代码分别如何调试？
    - 是否会使用 VS Code 断点调试？
    - 除浏览器控制台外，如何调试 Node.js 后端？
- 浏览器 Network 面板中常出现的 OPTIONS 请求是什么、有什么作用？
- Chrome DevTools 的 Network 面板中，各类筛选项、Header、请求信息分别用来做什么？
- Network 面板的 Timing / 时间轴如何分析？如何判断一个页面或请求到底慢在哪里？
- 你现在怎么写测试？实际写过测试吗？
- 了解 Playwright 吗？它解决什么问题、如何工作？
    - 什么是E2E测试？
    - 如何模拟用户操作并验证完整流程？
- 数据库掌握到什么程度？使用过哪些数据库？
- MySQL 的 MVCC 是什么？事务隔离有什么要求？
- 什么情况下应该建索引，什么情况下不适合建索引？
- 什么是事务？如何发起和管理一次数据库事务？
- GIS / 地理信息专业中常见哪些数据结构与算法？
- 了解哪些开源 GIS 系统？
- 它们使用了哪些关键算法或数据结构？
- 什么是 R 树（R-tree）？如何用于空间索引？
- 最短路径算法（如 Dijkstra）原理是什么，在实际系统中如何优化？
- 如果需要并发处理，思路是什么？
- JavaScript 的单线程与事件循环是怎么工作的？
    - 宏任务、微任务的执行顺序是什么？
    - 事件循环由谁实现？执行时机是否完全由 JS 规范决定？
- 了解 Generator / Async Generator 吗？
    - 解决什么问题？
    - yield 之后的执行是立即发生还是进入队列？
- 了解哪些设计模式？
- 什么是装饰器模式？
    - 从技术原理上看，它有哪些不变部分、哪些可扩展 / 可变化部分？
- 字节训练营属于什么性质？是培训、实习还是项目实践？
    - 是否需要筛选？
    - 参与它的目的是什么？
- 在学校的排名大概是多少？
- 平时有哪些其他活动或兴趣爱好？

### 反问
- 用友主做BIP和ERP，部门负责什么工作
- AI Coding在当前业务中的使用程度