---
title: 240.搜索二维矩阵II
description: 编写一个高效的算法来搜索mxn矩阵matrix中的一个目标值target。
sidebar_position: 25
tags: [LeetCode,数组,二分查找,分治,矩阵]
date: 2026-03-24
---

# 240.搜索二维矩阵II
编写一个高效的算法来搜索`m x n`矩阵`matrix`中的一个目标值`target`。该矩阵具有以下特性：
- 每行的元素从左到右升序排列。
- 每列的元素从上到下升序排列。

示例1：
![240-1](https://blog-1385521233.cos.ap-guangzhou.myqcloud.com/docs/algorithm/240-1.jpg)
- **输入**：matrix = [[1,4,7,11,15],[2,5,8,12,19],[3,6,9,16,22],[10,13,14,17,24],[18,21,23,26,30]], target = 5
- **输出**：true

示例2：
![240-2](https://blog-1385521233.cos.ap-guangzhou.myqcloud.com/docs/algorithm/240-2.jpg)
- **输入**：matrix = [[1,4,7,11,15],[2,5,8,12,19],[3,6,9,16,22],[10,13,14,17,24],[18,21,23,26,30]], target = 20
- **输出**：false

## 解题思路
### 1.右上角搜索
题干说明了这个矩阵具备的两个特性：每行的元素从左到右升序排列，每列的元素从上到下升序排列。那么可以想到如果从矩形的右上角入手，如果当前元素等于目标值，则直接返回 true；如果当前元素大于目标值，则说明目标值不可能在当前列中，应该向左移动；如果当前元素小于目标值，则说明目标值不可能在当前行中，应该向下移动。这样不断地进行比较和移动，直到找到目标值或者超出矩阵的边界。这个思路实际上也可以从左下角开始做搜索。

时间复杂度：O(m+n)，其中 m 和 n 分别是 matrix 的行数和列数。

```ts
function searchMatrix(matrix: number[][], target: number): boolean {
    const row = matrix.length
    const col = matrix[0].length
    
    let i = 0;
    let j = col - 1

    while (i < row && j >= 0) {
        if (matrix[i][j] === target) {
            return true
        } else if (matrix[i][j] < target) {
            i++
        } else {
            j--
        }
    }

    return false
};
```

## 总结