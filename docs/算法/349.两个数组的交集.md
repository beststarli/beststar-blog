---
title: 349.两个数组的交集
description: 给定两个数组nums1和nums2，返回它们的交集。输出结果中的每个元素一定是唯一的。我们可以不考虑输出结果的顺序。
sidebar_position: 134
tags: [LeetCode,数组,哈希表,双指针,二分查找,简单]
date: 2026-06-30
---

# [349.两个数组的交集](https://leetcode.cn/problems/intersection-of-two-arrays/description/)
给定两个数组 `nums1` 和 `nums2` ，返回 *它们的* 交集。输出结果中的每个元素一定是唯一的。我们可以 **不考虑输出结果的顺序**。

:::info **交集**
The intersection of two arrays is defined as the set of elements that are present in both arrays.
:::

示例1：
- **输入**：nums1 = [1,2,2,1], nums2 = [2,2]
- **输出**：[2]

示例2：
- **输入**：nums1 = [4,9,5], nums2 = [9,4,9,8,4]
- **输出**：[9,4]
- **解释**：[4,9] 也是可通过的

## 解题思路
### 1.两个哈希set去重
用一个set来存储`nums1`中的元素，然后遍历`nums2`，如果`nums2`中的元素在set中存在，则加入结果集，结果`ans`也用set去重，最后返回`ans`展开后的数组。

- 时间复杂度：O(n)
- 空间复杂度：O(n)

```ts
function intersection(nums1: number[], nums2: number[]): number[] {
    const numSet = new Set(nums1)
    const ans = new Set<number>()
    for (const num of nums2) {
        if (numSet.has(num)) {
            ans.add(num)
        }
    }

    return [...ans]
};
```

### 2.一个哈希set去重
借助set的`delete`方法，遍历`nums2`，如果`nums2`中的元素在set中存在，则加入结果数组，并从set中删除该元素，这样可以保证结果集中的元素唯一性。

- 时间复杂度：O(n)
- 空间复杂度：O(n)

```ts
function intersection(nums1: number[], nums2: number[]): number[] {
    const numSet = new Set(nums1)
    const ans = []
    for (const num of nums2) {
        if (numSet.delete(num)) {
            ans.push(num)
        }
    }

    return ans
};
```

## 总结