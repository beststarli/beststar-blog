---
title: 217.存在重复元素
description: 给你一个整数数组nums。如果任一值在数组中出现至少两次，返回true；如果数组中每个元素互不相同，返回false。
sidebar_position: 180
tags: [LeetCode,数组,哈希表,简单]
date: 2026-07-17
---

# [217.存在重复元素](https://leetcode.cn/problems/contains-duplicate/description/)   
给你一个整数数组 `nums` 。如果任一值在数组中出现 **至少两次** ，返回 `true` ；如果数组中每个元素互不相同，返回 `false` 。

示例1：
- **输入**：nums = [1,2,3,1]
- **输出**：true
- **解释**：元素 1 在下标 0 和 3 出现。

示例2：
- **输入**：nums = [1,2,3,4]
- **输出**：false
- **解释**：所有元素都不同。

示例3：
- **输入**：nums = [1,1,1,3,3,4,3,2,4,2]
- **输出**：true

## 解题思路
### 1.哈希set——写法一
将数组nums转为哈希set，如果set的size不等于nums的length，则说明有重复元素。

- 时间复杂度：O(n)，其中 n 是数组 nums 的长度。我们需要遍历数组一次来将元素添加到哈希集合中。
- 空间复杂度：O(n)，其中 n 是数组 nums 的长度。哈希集合需要存储数组中的所有元素。

```ts
function containsDuplicate(nums: number[]): boolean {
    const set = new Set(nums)
    return set.size !== nums.length
};
```

### 2.哈希set——写法二
遍历数组将数字加入哈希set，如果当前数字已经在set中出现过了，则说明有重复元素，返回true；如果遍历完数组后没有发现重复元素，则返回false。

- 时间复杂度：O(n)，其中 n 是数组 nums 的长度。我们需要遍历数组一次来将元素添加到哈希集合中。
- 空间复杂度：O(n)，其中 n 是数组 nums 的长度。哈希集合需要存储数组中的所有元素。

```ts
function containsDuplicate(nums: number[]): boolean {
    const set = new Set()
    for (const num of nums) {
        if (set.has(num)) {
            return true
        }
        set.add(num)
    }
    return false
};
```

## 总结