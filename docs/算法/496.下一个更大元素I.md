---
title: 496.下一个更大元素I
description: nums1中数字x的下一个更大元素是指x在nums2中对应位置右侧的第一个比x大的元素。
sidebar_position: 163
tags: [LeetCode,栈,数组,哈希表,单调栈,简单]
date: 2026-07-14
---

# [496.下一个更大元素I](https://leetcode.cn/problems/next-greater-element-i/description/)
`nums1` 中数字 `x` 的 下一个更大元素 是指 `x` 在 `nums2` 中对应位置 **右侧** 的 **第一个** 比 `x` 大的元素。

给你两个 **没有重复元素** 的数组 `nums1` 和 `nums2` ，下标从 **0** 开始计数，其中`nums1` 是 `nums2` 的子集。

对于每个 `0 <= i < nums1.length` ，找出满足 `nums1[i] == nums2[j]` 的下标 `j` ，并且在 `nums2` 中确定 `nums2[j]` 的 下一个更大元素 。如果不存在下一个更大元素，那么本次查询的答案是 `-1` 。

返回一个长度为 `nums1.length` 的数组 `ans` 作为答案，满足 `ans[i]` 是如上所述的 **下一个更大元素** .

示例1：
- **输入**：nums1 = [4,1,2], nums2 = [1,3,4,2].
- **输出**：[-1,3,-1]
- **解释**：nums1 中每个值的下一个更大元素如下所述：
```txt
- 4 ，用加粗斜体标识，nums2 = [1,3,4,2]。不存在下一个更大元素，所以答案是 -1 。
- 1 ，用加粗斜体标识，nums2 = [1,3,4,2]。下一个更大元素是 3 。
- 2 ，用加粗斜体标识，nums2 = [1,3,4,2]。不存在下一个更大元素，所以答案是 -1 。
```

示例2：
- **输入**：nums1 = [2,4], nums2 = [1,2,3,4].
- **输出**：[3,-1]
- **解释**：nums1 中每个值的下一个更大元素如下所述：
```txt
- 2 ，用加粗斜体标识，nums2 = [1,2,3,4]。下一个更大元素是 3 。
- 4 ，用加粗斜体标识，nums2 = [1,2,3,4]。不存在下一个更大元素，所以答案是 -1 。
```

**进阶**：你可以设计一个时间复杂度为 O(nums1.length + nums2.length) 的解决方案吗？

## 解题思路
### 1.数组记录下标——暴力
用一个数组来记录nums1中数字在nums2中的下标，然后从该下标开始向右遍历，找到第一个比该数字大的元素，如果没有则返回-1。

- 时间复杂度：O(n*m)，其中 n 是 nums1 的长度，m 是 nums2 的长度。
- 空间复杂度：O(n)，其中 n 是 nums1 的长度。

```ts
function nextGreaterElement(nums1: number[], nums2: number[]): number[] {
    const indexArr = []
    const ans = []
    for (const num of nums1) {
        indexArr.push(nums2.indexOf(num))
    }

    for (const index of indexArr) {
        let start = index + 1
        let bigger = nums2[index]
        while (start < nums2.length) {
            if (nums2[start] > nums2[index]) {
                bigger = nums2[start]
                ans.push(bigger)
                break
            }
            start++
        }
        if (bigger === nums2[index]) {
            ans.push(-1)
        }
    }
    return ans
};
```

### 2.单调栈——进阶
用一个哈希map来记录num1中元素对应的下标，用栈来记录还没算出下一个更大元素的那些数，只要遍历到nums2中有比栈顶元素更大的数，就将栈顶元素弹出，并将该元素对应的下一个更大元素记录在答案数组中，直到栈为空或者栈顶元素大于当前遍历的数。最后将当前遍历的数入栈。初始化答案数组被-1填充，这样就不用处理没有更大元素的数的情况了。

- 时间复杂度：O(n+m)，其中 n 是 nums1 的长度，m 是 nums2 的长度。
- 空间复杂度：O(m)，其中 m 是 nums2 的长度。

```ts
function nextGreaterElement(nums1: number[], nums2: number[]): number[] {
    const index = new Map()
    nums1.forEach((value, i) => index.set(value, i))
    const ans = Array(nums1.length).fill(-1)
    const stack = []
    for (const num of nums2) {
        while (stack.length > 0 && num > stack[stack.length - 1]) {
            ans[index.get(stack.pop())] = num
        }
        if (index.has(num)) {
            stack.push(num)
        }
    }
    return ans
};
```


## 总结