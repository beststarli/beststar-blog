---
title: 703.数据流中的第K大元素
description: 设计一个找到数据流中第k大元素的类（class）。注意是排序后的第k大元素，不是第k个不同的元素。
sidebar_position: 204
tags: [LeetCode,树,二叉搜索树,二叉树,堆（优先队列）,简单]
date: 2026-07-28
---

# [703.数据流中的第K大元素](https://leetcode.cn/problems/kth-largest-element-in-a-stream/description/)
设计一个找到数据流中第 k 大元素的类（class）。注意是排序后的第 `k` 大元素，不是第 `k` 个不同的元素。

请实现 `KthLargest` 类：
- `KthLargest(int k, int[] nums)` 使用整数 `k` 和整数流 `nums` 初始化对象。
- `int add(int val)` 将 `val` 插入数据流 `nums` 后，返回当前数据流中第 `k` 大的元素。

示例1：
- **输入**：
```txt
["KthLargest", "add", "add", "add", "add", "add"]
[[3, [4, 5, 8, 2]], [3], [5], [10], [9], [4]]
```
- **输出**：[null, 4, 5, 5, 8, 8]
- **解释**：
```txt
KthLargest kthLargest = new KthLargest(3, [4, 5, 8, 2]);
kthLargest.add(3); // 返回 4
kthLargest.add(5); // 返回 5
kthLargest.add(10); // 返回 5
kthLargest.add(9); // 返回 8
kthLargest.add(4); // 返回 8
```

示例2：
- **输入**：
```txt
["KthLargest", "add", "add", "add", "add"]
[[4, [7, 7, 7, 7, 8, 3]], [2], [10], [9], [9]]
```
- **输出**：[null, 7, 7, 7, 8]
- **解释**：
```txt
KthLargest kthLargest = new KthLargest(4, [7, 7, 7, 7, 8, 3]);
kthLargest.add(2); // 返回 7
kthLargest.add(10); // 返回 7
kthLargest.add(9); // 返回 7
kthLargest.add(9); // 返回 8
```

## 解题思路
### 1.手写小顶堆
因为通过插上数据，原数组的长度是变化的，但是我们始终关心的是前K个大的数，至于更小的数我们并不关心，所以可以维护一个小顶堆，堆的大小就是K，那么堆顶元素就是第K大的元素。每次插入一个新元素时，如果堆的大小小于K，那么直接插入；如果堆的大小等于K，那么就比较堆顶元素和新元素的大小，如果新元素大于堆顶元素，那么就弹出堆顶元素，然后将新元素插入堆中。

- 时间复杂度：O(logK)，其中 K 是堆的大小。每次插入新元素时，可能需要调整堆的结构，调整堆的时间复杂度为 O(logK)。
- 空间复杂度：O(K)，因为我们只需要维护一个大小为 K 的小顶堆。

```ts
class MyMinHeap {
    private arr

    constructor() {
        this.arr = []
    }

    down(i) {
        const left = 2 * i + 1
        const right = 2 * i + 2
        let mostMin = i

        if (left < this.arr.length && this.arr[left] < this.arr[mostMin]) {
            mostMin = left
        }
        if (right < this.arr.length && this.arr[right] < this.arr[mostMin]) {
            mostMin = right
        }
        if (mostMin !== i) {
            [this.arr[mostMin], this.arr[i]] = [this.arr[i], this.arr[mostMin]]
            this.down(mostMin)
        }
    }

    up(i) {
        if (i === 0) {
            return
        }
        const father = Math.floor((i - 1) / 2)
        if (father >= 0 && this.arr[father] > this.arr[i]) {
            [this.arr[father], this.arr[i]] = [this.arr[i], this.arr[father]]
            this.up(father)
        }
    }

    insert(val) {
        this.arr.push(val)
        const id = this.arr.length - 1
        this.up(id)
    }

    shift() {
        if (this.arr.length === 0) {
            return null
        }
        if (this.arr.length === 1) {
            return this.arr.pop()
        }
        const res = this.arr[0]
        const last = this.arr.pop()
        this.arr[0] = last
        this.down(0)
        return res
    }

    size() {
        return this.arr.length
    }

    top() {
        return this.arr[0]
    }
}

class KthLargest {
    private heap
    private k

    constructor(k: number, nums: number[]) {
        this.heap = new MyMinHeap()
        this.k = k
        nums.forEach(num => this.add(num))
    }

    add(val: number): number {
        if (this.heap.size() < this.k) {
            this.heap.insert(val)
        } else if (val > this.heap.top()) {
            this.heap.shift()
            this.heap.insert(val)
        }
        return this.heap.top()
    }
}

/**
 * Your KthLargest object will be instantiated and called as such:
 * var obj = new KthLargest(k, nums)
 * var param_1 = obj.add(val)
 */
```


## 总结
### 二叉树节点关系
对于二叉树中某个节点`i`，它的左子树节点是`2 * i + 1`，右子树节点是`2 * i + 2`，父节点是`Math.floor((i - 1) / 2)`。

### 大顶堆数据结构怎么写
相较于小顶堆，只需要修改up()和down()中的比较符号方向即可。
```ts
class MyMaxHeap {
    private arr

    constructor() {
        this.arr = []
    }

    down(i) {
        const left = 2 * i + 1
        const right = 2 * i + 2
        let mostMax = i

        if (left < this.arr.length && this.arr[left] > this.arr[mostMax]) {
            mostMax = left
        }
        if (right < this.arr.length && this.arr[right] > this.arr[mostMax]) {
            mostMax = right
        }
        if (mostMax !== i) {
            [this.arr[mostMax], this.arr[i]] = [this.arr[i], this.arr[mostMax]]
            this.down(mostMax)
        }
    }

    up(i) {
        if (i === 0) {
            return
        }
        const father = Math.floor((i - 1) / 2)
        if (father >= 0 && this.arr[father] < this.arr[i]) {
            [this.arr[father], this.arr[i]] = [this.arr[i], this.arr[father]]
            this.up(father)
        }
    }

    insert(val) {
        this.arr.push(val)
        const id = this.arr.length - 1
        this.up(id)
    }

    shift() {
        if (this.arr.length === 0) {
            return null
        }
        if (this.arr.length === 1) {
            return this.arr.pop()
        }
        const res = this.arr[0]
        const last = this.arr.pop()
        this.arr[0] = last
        this.down(0)
        return res
    }

    size() {
        return this.arr.length
    }

    top() {
        return this.arr[0]
    }
}
```