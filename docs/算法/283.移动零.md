---
title: 283.移动零
description: 给定一个数组nums，编写一个函数将所有0移动到数组的末尾，同时保持非零元素的相对顺序。
sidebar_position: 7
tags: [LeetCode,双指针,简单]
date: 2026-01-12
---

# 283.移动零
给定一个数组`nums`，编写一个函数将所有`0`移动到数组的末尾，同时保持非零元素的相对顺序。

**请注意**，必须在不复制数组的情况下原地对数组进行操作。

示例1：
- **输入**：nums = [0,1,0,3,12]
- **输出**：[1,3,12,0,0]

示例2：
- **输入**：nums = [0]
- **输出**：[0]

## 解题思路
### 个人题解
#### 1.先删除再添加
先遍历数组将是0的元素删除，实际就是将非零元素向前移动，记录0的个数，最后在数组末尾补充对应个数的0即可。

实际实现中是记录非零元素的个数，再以非零元素长度为起点添加0直至原数组末尾。

时间复杂度：O(n)
```ts
function moveZeroes(nums: number[]): void {
    if (nums.length === 0) return

    let noZeroCount = 0

    for (let i = 0; i < nums.length; i++) {
        if (nums[i] !== 0) {
            nums[noZeroCount++] = nums[i]
        }
    }

    for (let i = noZeroCount; i < nums.length; i++) {
        nums[i] = 0
    }
}
```

#### 2.直接移动0

时间复杂度：O(n)
```ts
function moveZeroes(nums: number[]): void {
    let i = 0
    let j = 0
    while ( i < nums.length ) {
        if (nums[i] === 0) {
            j++
        } else if (j > 0) {
            nums[i-j] = nums[i]
            nums[i] = 0
        }
        i++
    }
}
```

### 官方题解

## 总结