---
title: 80.删除有序数组中的重复项II
description: 给你一个有序数组nums，请你原地删除重复出现的元素，使得出现次数超过两次的元素只出现两次，返回删除后数组的新长度。
sidebar_position: 228
tags: [LeetCode,数组,双指针,中等]
date: 2026-08-25
---

# [80.删除有序数组中的重复项II](https://leetcode.cn/problems/remove-duplicates-from-sorted-array-ii/description/?envType=study-plan-v2&envId=top-interview-150)
给你一个有序数组 `nums` ，请你 **原地** 删除重复出现的元素，使得出现次数超过两次的元素**只出现两次** ，返回删除后数组的新长度。

不要使用额外的数组空间，你必须在 **原地** 修改输入数组 并在使用 O(1) 额外空间的条件下完成。

说明：

为什么返回数值是整数，但输出的答案是数组呢？

请注意，输入数组是以「引用」方式传递的，这意味着在函数里修改输入数组对于调用者是可见的。

你可以想象内部操作如下:
```txt
// nums 是以“引用”方式传递的。也就是说，不对实参做任何拷贝
int len = removeDuplicates(nums);

// 在函数里修改输入数组对于调用者是可见的。
// 根据你的函数返回的长度, 它会打印出数组中 该长度范围内 的所有元素。
for (int i = 0; i < len; i++) {
    print(nums[i]);
}
```

示例1：
- **输入**：nums = [1,1,1,2,2,3]
- **输出**：5, nums = [1,1,2,2,3]
- **解释**：函数应返回新长度 length = 5, 并且原数组的前五个元素被修改为 1, 1, 2, 2, 3。 不需要考虑数组中超出新长度后面的元素。

示例2：
- **输入**：nums = [0,0,1,1,1,1,2,3,3]
- **输出**：7, nums = [0,0,1,1,2,3,3]
- **解释**：函数应返回新长度 length = 7, 并且原数组的前七个元素被修改为 0, 0, 1, 1, 2, 3, 3。不需要考虑数组中超出新长度后面的元素。

## 解题思路
### 1.栈模拟
既然题意是要求输出的数组中每个元素最多出现两次，那么怎么保证这个条件实现呢？我们其实只需要关注最近新加入“结果数组”的两个元素以及当前元素即可，如果当前遍历元素与结果数组中最近的两个元素相同，那么说明这个元素的数量够了不能加入结果数组，否则就加入结果数组。于是在原数组上我们可以用栈来模拟这个过程，比较栈顶之下的那个元素和当前元素是否相同，如果相同就不加入结果数组，否则就加入结果数组。

注意初始化时栈的大小为2，保证栈中有两个元素可以比较。但是原题数组的长度不小于2，所以要特殊处理数组长度小于2的情况。

- 时间复杂度：O(n)
- 空间复杂度：O(1)

```ts
function removeDuplicates(nums: number[]): number {
    if (nums.length === 1) {
        return nums.length
    }
    let stackSize = 2
    for (let i = 2; i < nums.length; i++) {
        if (nums[i] !== nums[stackSize - 2]) {
            nums[stackSize] = nums[i]
            stackSize++
        }
    }
    return stackSize
};
```

## 总结