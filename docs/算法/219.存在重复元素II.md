---
title: 219.存在重复元素II
description: 给你一个整数数组nums和一个整数k，判断数组中是否存在两个不同的索引i和j，满足nums[i] == nums[j]且abs(i - j) <= k。如果存在，返回true；否则，返回false。
sidebar_position: 143
tags: [LeetCode,数组,哈希表,滑动窗口,简单]
date: 2026-07-09
---

# [219.存在重复元素II](https://leetcode.cn/problems/contains-duplicate-ii/description/)
给你一个整数数组 `nums` 和一个整数 `k` ，判断数组中是否存在两个 不同的索引 `i` 和 `j` ，满足 `nums[i] == nums[j]` 且 `abs(i - j) <= k`。如果存在，返回 `true` ；否则，返回 `false` 。

示例1：
- **输入**：nums = [1,2,3,1], k = 3
- **输出**：true

示例2：
- **输入**：nums = [1,0,1,1], k = 1
- **输出**：true

示例3：
- **输入**：nums = [1,2,3,1,2,3], k = 2
- **输出**：false

## 解题思路
### 1.哈希map
用一个哈希map来存储已遍历的数字作为键，当前的下标以及与前一次出现的下标的差值作为值，如果这个数字是第一次遍历到则将差值初始化为Infinity，方便比较大小时取更小值，当能够计算差值时，将差值直接与K比较，小于等于则直接返回true，否则在遍历结束后返回false。

- 时间复杂度：O(n)，其中 n 是数组 nums 的长度。我们只需要遍历一次数组。
- 空间复杂度：O(n)，其中 n 是数组 nums 的长度。最坏情况下，哈希表中可能会存储所有的元素。

```ts
function containsNearbyDuplicate(nums: number[], k: number): boolean {
    const indexMap = new Map()
    for (let i = 0; i < nums.length; i++) {
        if (!indexMap.has(nums[i])) {
            indexMap.set(nums[i], [i, Infinity])
        } else {
            const preIndex = indexMap.get(nums[i])
            const minDist = Math.min(preIndex[1], i - preIndex[0])
            if (minDist <= k) {
                return true
            }
            indexMap.set(nums[i], [i, minDist])
        }
    }
    return false
};
```

### 2.哈希map优化
事实上针对解法一的代码还可以再优化逻辑，当遍历到的数字新的下标出现时，先前更远的下标就不再需要了，所以可以直接将当前下标覆盖掉之前的下标，这样就不需要存储差值了，直接比较当前下标与之前下标的差值是否小于等于K即可。

- 时间复杂度：O(n)，其中 n 是数组 nums 的长度。我们只需要遍历一次数组。
- 空间复杂度：O(n)，其中 n 是数组 nums 的长度。最坏情况下，哈希表中可能会存储所有的元素。

```ts
function containsNearbyDuplicate(nums: number[], k: number): boolean {
    const map = new Map()
    for (let i = 0; i < nums.length; i++) {
        const prev = map.get(nums[i]) ?? -1
        if (prev !== -1 && i - prev <= k) {
            return true
        }
        map.set(nums[i], i)
    }
    return false
};
```

### 3.滑动窗口
使用滑动窗口的方式来解决这个问题，维护一个大小为k的窗口，当遍历到一个新的数字时，先判断这个数字是否在窗口中，如果在则返回true，否则将这个数字加入窗口中，如果窗口的大小超过k，则移除最早加入的数字。窗口头部的数字是nums[i - k - 1]，窗口尾部的数字是nums[i]。

- 时间复杂度：O(n)，其中 n 是数组 nums 的长度。我们只需要遍历一次数组。
- 空间复杂度：O(k)，其中 k 是窗口的大小。最坏情况下，窗口中可能会存储k个不同的元素。

```ts
function containsNearbyDuplicate(nums: number[], k: number): boolean {
    const window = new Set()
    for (let i = 0; i < nums.length; i++) {
        if (i > k) {
            window.delete(nums[(i - k) - 1])
        }
        if (window.has(nums[i])) {
            return true
        }
        window.add(nums[i])
    }
    return false
};
```

## 总结