---
title: 977.有序数组的平方
description: 给你一个按非递减顺序排序的整数数组nums，返回每个数字的平方组成的新数组，要求也按非递减顺序排序。
sidebar_position: 149
tags: [LeetCode,数组,双指针,简单]
date: 2026-07-10
---

# [977.有序数组的平方](https://leetcode.cn/problems/squares-of-a-sorted-array/description/)
给你一个按 **非递减顺序** 排序的整数数组 `nums`，返回 **每个数字的平方** 组成的新数组，要求也按 **非递减顺序** 排序。

示例1：
- **输入**：nums = [-4,-1,0,3,10]
- **输出**：[0,1,9,16,100]
- **解释**：
```txt
平方后，数组变为 [16,1,0,9,100]
排序后，数组变为 [0,1,9,16,100]
```

示例2：
- **输入**：nums = [-7,-3,2,3,11]
- **输出**：[4,9,9,49,121]

**进阶**：
- 请你设计时间复杂度为 `O(n)` 的算法解决本问题

## 解题思路
### 1.双指针
维护双指针，分别指向数组的首尾，比较两端的绝对值大小，将较大的平方放入结果数组中，并移动对应的指针，直到两个指针相遇。然后将结果数组反转即可。

- 时间复杂度：O(n)，其中 n 是数组的长度。每个元素恰好被访问一次。
- 空间复杂度：O(n)，其中 n 是数组的长度。空间主要为结果数组的开销。

```ts
function sortedSquares(nums: number[]): number[] {
    let left = 0
    let right = nums.length - 1
    const ans = []
    while (left <= right) {
        if (Math.abs(nums[left]) < Math.abs(nums[right])) {
            ans.push(nums[right] ** 2)
            right--
        } else {
            ans.push(nums[left] ** 2)
            left++
        }
    }
    return ans.reverse()
};
```

## 总结