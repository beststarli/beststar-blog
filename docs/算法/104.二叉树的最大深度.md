---
title: 104.二叉树的最大深度
description: 给定一个二叉树root，返回其最大深度。
sidebar_position: 45
tags: [LeetCode,树,深度优先搜索,广度优先搜索,二叉树,简单]
date: 2026-04-02
---

# 104.二叉树的最大深度
给定一个二叉树的根节点`root`，返回其最大深度。

二叉树的 **最大深度** 是指从根节点到最远叶子节点的最长路径上的节点数。

示例1：
![104](https://blog-1385521233.cos.ap-guangzhou.myqcloud.com/docs/algorithm/104.jpg)
- **输入**：root = [3,9,20,null,null,15,7]
- **输出**：3

示例2：
- **输入**：root = [1,null,2]
- **输出**：2

## 解题思路
### 1.递归——自底向上
对根结点的左右子树分别应用递归，得到左右子树的最大深度，取两者的较大值加1即为当前结点的最大深度。递归结束的条件是当前结点为null，此时返回0。

时间复杂度：O(n)，其中n是二叉树的结点数。每个结点访问一次。

```ts
/**
 * Definition for a binary tree node.
 * class TreeNode {
 *     val: number
 *     left: TreeNode | null
 *     right: TreeNode | null
 *     constructor(val?: number, left?: TreeNode | null, right?: TreeNode | null) {
 *         this.val = (val===undefined ? 0 : val)
 *         this.left = (left===undefined ? null : left)
 *         this.right = (right===undefined ? null : right)
 *     }
 * }
 */

function maxDepth(root: TreeNode | null): number {
    if (root === null) return 0

    let leftDepth = maxDepth(root.left)
    let rightDepth = maxDepth(root.right)
    
    const max = Math.max(leftDepth, rightDepth) + 1

    return max
};
```

### 2.迭代——自顶向下
迭代的思路是层序遍历二叉树，记录每一层的结点数，每访问完一层就将深度加1，直到访问完所有层为止，此时的深度即为二叉树的最大深度。

时间复杂度：O(n)，其中n是二叉树的结点数。

```ts
/**
 * Definition for a binary tree node.
 * class TreeNode {
 *     val: number
 *     left: TreeNode | null
 *     right: TreeNode | null
 *     constructor(val?: number, left?: TreeNode | null, right?: TreeNode | null) {
 *         this.val = (val===undefined ? 0 : val)
 *         this.left = (left===undefined ? null : left)
 *         this.right = (right===undefined ? null : right)
 *     }
 * }
 */

function maxDepth(root: TreeNode | null): number {
    let ans = 0;
    function dfs(node, depth) {
        if (node === null) {
            return;
        }
        depth++;
        ans = Math.max(ans, depth);
        dfs(node.left, depth);
        dfs(node.right, depth);
    };
    dfs(root, 0);
    return ans;
};
```

## 总结