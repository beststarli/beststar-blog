---
title: 146.LRU缓存
description: 请你设计并实现一个满足  LRU (最近最少使用) 缓存 约束的数据结构。
sidebar_position: 42
tags: [LeetCode,链表,分治,堆(优先队列),归并排序,困难]
date: 2026-04-02
---

# 146.LRU缓存

示例：
- **输入**：
```txt
["LRUCache", "put", "put", "get", "put", "get", "put", "get", "get", "get"]
[[2], [1, 1], [2, 2], [1], [3, 3], [2], [4, 4], [1], [3], [4]]
```
- **输出**：[null, null, null, 1, null, -1, null, -1, 3, 4]
- **解释**：
```txt
LRUCache lRUCache = new LRUCache(2);
lRUCache.put(1, 1); // 缓存是 {1=1}
lRUCache.put(2, 2); // 缓存是 {1=1, 2=2}
lRUCache.get(1);    // 返回 1
lRUCache.put(3, 3); // 该操作会使得关键字 2 作废，缓存是 {1=1, 3=3}
lRUCache.get(2);    // 返回 -1 (未找到)
lRUCache.put(4, 4); // 该操作会使得关键字 1 作废，缓存是 {4=4, 3=3}
lRUCache.get(1);    // 返回 -1 (未找到)
lRUCache.get(3);    // 返回 3
lRUCache.get(4);    // 返回 4
```

## 解题思路
### 1.手写双向链表
解法参考自[灵茶山艾府](https://leetcode.cn/problems/lru-cache/solutions/2456294/tu-jie-yi-zhang-tu-miao-dong-lrupythonja-czgt/?envType=study-plan-v2&envId=top-100-liked)。

![146](https://blog-1385521233.cos.ap-guangzhou.myqcloud.com/docs/algorithm/146.png)

时间复杂度：O(1)，其中n是缓存的容量。

```ts
class LRUCache {
    private capacity
    private dummy: Node;
    private keyToNode: Map<number, Node>;

    constructor(capacity: number) {
        this.capacity = capacity
        this.dummy = new Node()
        this.dummy.prev = this.dummy
        this.dummy.next = this.dummy
        this.keyToNode = new Map()
    }

    // 获取 key 对应的节点，同时把该节点移到链表头部
    _getNode(key) {
        if (!this.keyToNode.has(key)) { // 没有这本书
            return null;
        }
        const node = this.keyToNode.get(key); // 有这本书
        this._remove(node); // 把这本书抽出来
        this._pushFront(node); // 放到最上面
        return node;
    }

    get(key: number): number {
        const node = this._getNode(key); // getNode 会把对应节点移到链表头部
        return node ? node.value : -1;
    }

    put(key: number, value: number): void {
        let node = this._getNode(key); // getNode 会把对应节点移到链表头部
        if (node) { // 有这本书
            node.value = value; // 更新 value
            return;
        }
        node = new Node(key, value) // 新书
        this.keyToNode.set(key, node);
        this._pushFront(node); // 放到最上面
        if (this.keyToNode.size > this.capacity) { // 书太多了
            const backNode = this.dummy.prev;
            this.keyToNode.delete(backNode.key);
            this._remove(backNode); // 去掉最后一本书
        }
    }

    // 删除一个节点（抽出一本书）
    _remove(x) {
        x.prev.next = x.next;
        x.next.prev = x.prev;
    }

    // 在链表头添加一个节点（把一本书放到最上面）
    _pushFront(x) {
        x.prev = this.dummy;
        x.next = this.dummy.next;
        x.prev.next = x;
        x.next.prev = x;
    }
}

class Node {
    key: number;
    value: number;
    prev: Node | null;
    next: Node | null;

    constructor(key = 0, value = 0) {
        this.key = key
        this.value = value
        this.prev = null
        this.next = null
    }
}

/**
 * Your LRUCache object will be instantiated and called as such:
 * var obj = new LRUCache(capacity)
 * var param_1 = obj.get(key)
 * obj.put(key,value)
 */
```


## 总结
具体可以参考[LRU是什么](/docs/算法/LRU是什么)。

一句话总结LRU的作用：当公司没有hc时，为了降本增效，如果有年轻牛马加入，公司得先把不中用的老东西开了。