---
title: 17.电话号码的字母组合
description: 给定一个仅包含数字2-9的字符串，返回所有它能表示的字母组合。答案可以按任意顺序返回。
sidebar_position: 75
tags: [LeetCode,哈希表,字符串,回溯,中等]
date: 2026-04-18
---

# 17.电话号码的字母组合
给定一个仅包含数字`2-9`的字符串，返回所有它能表示的字母组合。答案可以按 **任意顺序** 返回。

给出数字到字母的映射如下（与电话按键相同）。注意`1`不对应任何字母。

![17-1](https://blog-1385521233.cos.ap-guangzhou.myqcloud.com/docs/algorithm/17-1.png)

示例1：
- **输入**：digits = "23"
- **输出**：["ad","ae","af","bd","be","bf","cd","ce","cf"]

示例2：
- **输入**：digits = "2"
- **输出**：["a","b","c"]

## 解题思路
### 1.对可选项递归
注意到最终可能结果的每个位次的字母都是由输入数字对应的字母集合中的一个字母组成的，只要对每一位次做可选字母的遍历递归即可。我们可以使用一个数组`path`来记录当前的路径，递归结束后将路径加入结果中。在递归过程中直接覆盖`path`数组中对应位次的字母即可，无需恢复现场。

时间复杂度：O(4^n)。

```ts
function letterCombinations(digits: string): string[] {
    const MAPPING = ["", "", "abc", "def", "ghi", "jkl", "mno", "pqrs", "tuv", "wxyz"]

    if (digits.length === 0) {
        return []
    }

    const path = Array(digits.length) // 可选项树的路径
    const ans = []

    function dfs(index: number) {
        if (index === digits.length) {
            ans.push(path.join(''))
            return 
        }
        const letters = MAPPING[Number(digits[index])]
        for (const letter of letters) {
            path[index] = letter  // 直接覆盖
            dfs(index + 1)
        }
    }

    dfs(0)
    return ans
};
```

## 总结