---
title: 295.数据流的中位数
description: 中位数是有序整数列表中的中间值。如果列表的大小是偶数，则没有中间值，中位数是两个中间值的平均值。
sidebar_position: 94
tags: [LeetCode,双指针,排序,堆（优先队列）,困难]
date: 2026-04-24
---

# 295.数据流的中位数
中位数是有序整数列表中的中间值。如果列表的大小是偶数，则没有中间值，中位数是两个中间值的平均值。
- 例如`arr = [2,3,4]`的中位数是`3`。
- 例如`arr = [2,3]`的中位数是`(2 + 3) / 2 = 2.5`。

实现`MedianFinder`类:
- `MedianFinder()`初始化`MedianFinder`对象。
- `void addNum(int num)`将数据流中的整数`num`添加到数据结构中。
- `double findMedian()`返回到目前为止所有元素的中位数。与实际答案相差$10^5$以内的答案将被接受。

示例1：
- **输入**：
```txt
["MedianFinder", "addNum", "addNum", "findMedian", "addNum", "findMedian"]
[[], [1], [2], [], [3], []]
```
- **输出**：[null, null, null, 1.5, null, 2.0]
- **解释**：
```txt
MedianFinder medianFinder = new MedianFinder();
medianFinder.addNum(1);    // arr = [1]
medianFinder.addNum(2);    // arr = [1, 2]
medianFinder.findMedian(); // 返回 1.5 ((1 + 2) / 2)
medianFinder.addNum(3);    // arr[1, 2, 3]
medianFinder.findMedian(); // return 2.0
```

## 解题思路
### 1.大小顶堆
解法参考自[卡乐咪粽子](https://leetcode.cn/problems/find-median-from-data-stream/solutions/1697017/by-1105389168-3r4x/?envType=study-plan-v2&envId=top-100-liked)，[原题解](https://ricepudding.cn/article/2eb98691)。

![295-1](https://blog-1385521233.cos.ap-guangzhou.myqcloud.com/docs/algorithm/295-1.jpg)

维护两个堆：一个大顶堆`maxHeap`，一个小顶堆`minHeap`。大顶堆存储较小的一半数字，小顶堆存储较大的一半数字。这样就可以保证：
- 当数据流中的元素个数为偶数时，两个堆的大小相等，此时中位数为两个堆顶元素的平均值。
- 当数据流中的元素个数为奇数时，保证两个堆的长度相差1，那么长度较大的堆顶元素就是中位数。

**需理解本题如何建堆**。

时间复杂度：O(log n)，其中 n 是数据流中元素的数量。

```ts
class myHeap {
    public arr: Array<number>
    public compare: Function

    constructor(compare) {
        this.arr = [0]  // 忽略 0 这个索引,方便计算左右节点
        this.compare = compare ? compare : (a, b) => a > b  // 默认是大顶堆
    }

    get size() {
        return this.arr.length - 1
    }
    // 新增元素
    push(item) {
        this.arr.push(item)
        this.shiftUp(this.arr.length - 1)
    }
    // 元素上浮，k 是索引
    shiftUp(k) {
        let { arr, compare, parent } = this
        // 当前元素 > 父元素，则进行交换
        while (k > 1 && compare(arr[k], arr[parent(k)])) {
            this.swap(parent(k), k)
            k = parent(k)   // 更新 k 的位置为父元素的位置（交换了位置）
        }
    }
    // 弹出堆顶
    pop() {
        if (this.arr.length === 1) {
            return null
        }
        this.swap(1, this.arr.length - 1)   // 将结尾元素和堆顶元素交换位置
        let head = this.arr.pop()   // 删除堆顶
        this.sinkDown(1)    // 再做下沉操作
        return head
    }
    // 元素下沉
    sinkDown(k) {
        let { arr, compare, left, right, size } = this
        while (left(k) <= size) {
            // 1. 拿到左右节点的最大值
            let child = left(k)
            if (right(k) <= size && compare(arr[right(k)], arr[child])) {
                child = right(k)
            }
            // 2. k 满足大顶堆或小顶堆，什么都不做
            if (compare(arr[k], arr[child])) {
                return
            }
            // 3. 交换位置后，继续下沉操作
            this.swap(k, child)
            k = child   // 更新位置
        }
    }
    // 获取堆顶元素
    peak() {
        return this.arr[1]
    }
    // 获取左边元素节点
    left(k) {
        return k * 2
    }
    // 获取右边元素节点
    right(k) {
        return k * 2 + 1
    }   
    // 获取父节点
    parent(k) {
        return Math.floor(k >> 1)
    }
    // i、j 交换位置
    swap(i, j) {
        [this.arr[i], this.arr[j]] = [this.arr[j], this.arr[i]]
    }
}

class MedianFinder {
    public maxHeap: myHeap
    public minHeap: myHeap

    constructor() {
        // 大顶堆 放小的那一半数字
        this.maxHeap = new myHeap((a, b) => a > b);
        // 小顶堆 放大的那一半数字
        this.minHeap = new myHeap((a, b) => a < b);
    }

    addNum(num: number): void {
        if (this.maxHeap.size === this.minHeap.size) {
            this.minHeap.push(num)
            this.maxHeap.push(this.minHeap.pop()!)  // 维护两个堆的平衡
        } else {
            this.maxHeap.push(num)
            this.minHeap.push(this.maxHeap.pop()!)  // 维护两个堆的平衡
        }
    }

    findMedian(): number {
        if ((this.maxHeap.size + this.minHeap.size) % 2 === 0) {
            return (this.maxHeap.peak() + this.minHeap.peak()) / 2;
        }
        return this.maxHeap.peak()
    }
}


/**
 * Your MedianFinder object will be instantiated and called as such:
 * var obj = new MedianFinder()
 * obj.addNum(num)
 * var param_2 = obj.findMedian()
 */
```

## 二刷
### 1.大小顶堆数据结构
在计算中位数时，事实上就是在维护的两个升序数组left和right中，分别取left的最大值和right的最小值。随着addNum不断地添加数字，需要保证：
- left的长度和right的长度尽量相等，并规定总个数为奇数时，left的长度比right的长度大1。
- left的所有元素都小于等于right的所有元素。

下面分类讨论：
- 如果当前left的长度和right的长度相等：
    - 如果添加的数字num比较大，则将num添加到right中，现在left的长度就比right的长度小1了，不符合前文的规定，所以可以把right的最小值取出添加到left中，这样仍可以保证left的所有元素都小于等于right中的元素。
    - 如果添加的数字较小就直接添加到left中即可。
    - 两种情况合并就是无论大小都先加入到right中，再把right的最小值取出加入到left中。
- 如果当前left的长度比right的长度大1：
    - 如果添加的数字比较大，则加到right中。
    - 如果添加的数字比较小，就加到left中，现在left的长度就比right的长度大2了，不符合前文的规定，所以可以把left的最大值取出添加到right中，这样仍可以保证left的所有元素都小于等于right中的元素。
    - 两种情况合并就是无论大小都先加入到left中，再把left的最大值取出加入到right中。

根据以上情况，在选择数据结构时，left可以使用大顶堆，right可以使用小顶堆：
- 如果当前有奇数个元素，中位数就是left的堆顶
- 如果当前有偶数个元素，中位数就是left的堆顶和right的堆顶的平均值

- 时间复杂度：O(log n)，其中 n 是数据流中元素的数量。
- 空间复杂度：O(n)，其中 n 是数据流中元素的数量。

```ts
class MedianFinder {
    private left: MaxPriorityQueue<number>
    private right: MinPriorityQueue<number>
    
    constructor() {
        this.left = new MaxPriorityQueue()
        this.right = new MinPriorityQueue()
    }

    addNum(num: number): void {
        if (this.left.size() === this.right.size()) {
            this.right.enqueue(num)
            this.left.enqueue(this.right.dequeue())
        } else {
            this.left.enqueue(num)
            this.right.enqueue(this.left.dequeue())
        }
    }

    findMedian(): number {
        if (this.left.size() > this.right.size()) {
            return this.left.front()
        }
        return (this.left.front() + this.right.front()) / 2
    }
}

/**
 * Your MedianFinder object will be instantiated and called as such:
 * var obj = new MedianFinder()
 * obj.addNum(num)
 * var param_2 = obj.findMedian()
 */
```

## 总结
本题的思路相对简单，难点在于ACM模式下如何手写实现堆数据结构。
