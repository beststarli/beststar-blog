---
title: 闭包实现计数器
description: 利用闭包中变量的私有性，实现一个计数器函数，每次调用返回当前计数值并自增。
sidebar_position: 86
tags: [JavaScript,闭包,场景题,手撕题]
date: 2026-08-03
---

利用闭包中变量的私有性，实现一个计数器函数，每次调用返回当前计数值并自增。
## 代码实现
```ts
function createCounter() {
  let count = 0
  return {
    increment: () => {
      count++
      return count
    },
    decrement: () => {
      count--
      return count 
    },
    getCount: () => {
      return count
    },
    reset: () => {
      count = 0
      return count
    }
  }
}

const counter = createCounter()
console.log(counter.increment()) // 输出: 1
console.log(counter.increment()) // 输出: 2
console.log(counter.decrement()) // 输出: 1
console.log(counter.getCount())  // 输出: 1
console.log(counter.reset())     // 输出: 0
```